#summary 改进列表|更新内容

= 改进列表 =
  一些关于语电通的功能，我会列在下面，如果修正好一个，就会划掉一个。如果有改进的想法，<b>请不要直接在下面回帖</b>，请到[https://code.google.com/p/yuchcaller/issues/list issue]页面提出你宝贵的建议和意见。

 == 正在改进... ==
  # ~~ 多地区使用同一个区号的时候查询错误 ~~
  # ~~ 自架谷歌日历、联系人、任务同步服务器 ~~
  # ~~ 更新电话数据库到 2013年3月 ~~

 == 0.3.96 ==
  # 修改让自带主题的来电界面，信息能够显示正常。（无法完成，但是 ~~ 加入了禁用来去电显示归属的选项 ~~ ）
  # ~~ 加入5.0 6.0系统也能调整来电、通话显示文字的偏移位置 ~~
  # ~~ 加入IP拨号功能（在拨号界面、联系人加入菜单，直接IP拨号） ~~
  # ~~ 改进颜色选择，不是RGB值，而是说明文字。~~
  # ~~ 6.0 来电显示字体有些不好看，应该是画上去的问题。 ~~
  # ~~ 增加通话结束之后显示通话时长的功能（可以配置，默认不显示） ~~
  # ~~ 增加手动更新、自动查找更新的选项，让用户自己选择是否自动查询更新。 ~~
  # 4.5版本去掉背景的图片绘制，直接填充纯色，以加快来电显示速度
  # ~~ 更新电话数据库到2013年1月（274209）~~
  # ~~ 加入同步联系人、日历和任务的功能，详细见[https://code.google.com/p/yuchcaller/wiki/Sync_Feature 这里]。~~ 